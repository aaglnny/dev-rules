# 通用手写风格规则

## 0. 核心目标

让 AI 生成的代码去掉明显的“机器感”，更像一个赶着上线、但经验足够的资深开发者写出来的生产级代码。

代码要高效、干净、接地气，贴合当前项目上下文。

不要像教科书一样写代码，不要为了展示“规范”而牺牲阅读节奏。

本规则不绑定具体编程语言，适用于前端、后端、移动端、脚本、工具类代码等常见开发场景。

## 1. 风格来源

所有新增或修改代码，优先参考当前项目中已有的人工代码风格。

重点参考：
- 同模块已有代码
- 同类型页面、组件、服务、工具类
- 最近维护过且仍在使用的文件
- 项目中反复出现的命名、分层、错误处理和注释习惯

目标不是制造低质量代码，而是让代码更像项目长期维护者手写出来的结果。

如果项目已有风格和通用最佳实践冲突，优先保持项目一致性；只有在明显影响正确性、稳定性或可维护性时才调整。

## 2. 绝对禁止 AI 标志性特征

### 2.1 禁止过度封装

不要为了一个仅使用一次的简单判断、简单转换、简单点击事件、简单数据组装，单独封装辅助函数。

禁止这类无意义封装：
- `checkIfValid()`
- `handleSimpleClick()`
- `buildCommonParams()`
- `processData()`
- `formatData()`

如果逻辑只有两三行，且只在当前位置使用，直接写在当前位置。

只有满足以下情况才抽函数、类、组件或模块：
- 被多个地方复用
- 当前代码块已经明显影响阅读
- 逻辑有明确业务含义
- 抽出来后能减少真实复杂度
- 需要隔离副作用、生命周期或外部依赖

不要把代码拆成一堆碎片化调用，让读代码的人来回跳。

### 2.2 禁止小学生式注释

不要在人尽皆知的代码行上写解释性注释。

禁止：
- `// 给变量赋值`
- `// 遍历列表`
- `// 点击按钮`
- `// 设置标题`
- `// 返回数据`
- `// 调用接口`

允许：
- 复杂商业规则
- 临时兼容逻辑
- 平台、框架或第三方库的坑点
- 历史遗留行为
- 不写注释会让下一个人误删的代码
- 性能、并发、事务、缓存等容易误判的取舍

注释必须短，直接说明“为什么这样写”，不要解释“这行代码在做什么”。

### 2.3 禁止全都要的防御性编程

不要在内部可控流程里层层包异常捕获、空值判断、类型判断、边界判断。

私有函数、模块内部事件、已知来源的数据，不要写成面向未知互联网输入的防御代码。

允许必要校验：
- 用户输入
- 网络响应
- 文件读写
- 数据库读写
- 跨模块传参
- 外部系统回调
- 第三方库返回值
- 权限、支付、登录等高风险流程

禁止为了显得稳健，把每一步都写成：
- `if (!xxx) return`
- `try { ... } catch { ... }`
- `typeof xxx === 'string'`
- `Array.isArray(xxx)`
- `xxx == null` 后再重复兜底

能由项目流程保证的数据，就相信项目流程。

### 2.4 允许短上下文空泛命名

在已知业务上下文内，允许使用 `data`、`result`、`temp`、`item`、`list`、`info`、`params`、`config`、`options` 这类短命名。

如果变量只在很小的局部代码块里使用，读者只需要知道数据形态即可，不强制补业务前缀。

允许：
- 循环里用 `item`
- 局部返回值用 `result`
- 临时变量用 `temp`
- 局部参数对象用 `params`
- 单一列表用 `list`

只有在以下场景才优先使用业务命名：
- 跨函数传递
- 多个同类型数据同时存在
- 变量生命周期较长
- 仅靠 `data`、`result` 已经看不出来源
- 错误日志、数据库操作、跨模块调用需要明确业务上下文

示例：
- 有一个列表时用 `list`，有两个列表时用 `userList`、`orderList`
- 有多个结果时用 `saveResult`、`uploadResult`
- 需要日志定位时用 `filePath`、`requestUrl`、`orderId`

命名原则：短上下文允许看数据形态，长链路才补业务语义。

### 2.5 单一业务文件内允许短方法名

在单一业务页面、单一服务、单一模块或单一职责文件内，允许使用短方法名表达当前文件的核心动作。

如果文件本身已经提供了明确业务上下文，方法名不需要重复文件名、模块名或业务名。

允许：
- `deletePendingItem()` 可以命名为 `delete()`
- `handleSubmitResult()` 可以命名为 `submit()`
- `saveCurrentRecord()` 可以命名为 `save()`
- `loadUserList()` 在用户列表文件内可以命名为 `load()`

原因：
- 当前文件已经说明业务场景
- 方法只在当前文件内部使用
- 读者不需要通过方法名重复确认业务归属
- 短方法名更接近手写代码习惯

只有在以下场景才使用完整业务方法名：
- 方法会被其他文件调用
- 当前文件里存在多个相似动作
- 短方法名会造成歧义
- 方法承担跨业务逻辑
- 日志、回调、路由、事件、接口暴露等需要明确业务语义

示例：
- 单文件内只有一个保存动作，用 `save()`
- 单文件内只有一个删除动作，用 `delete()`
- 单文件内只有一个开始动作，用 `start()`
- 同时存在删除用户、删除文件、删除记录时，用 `deleteUser()`、`deleteFile()`、`deleteRecord()`

命名原则：文件名已经表达业务时，方法名只表达动作；业务边界变宽时，再补充业务名。

## 3. 禁止过度工整

不要为了“看起来规范”而做以下事情：
- 不要把每个小代码块都拆成独立函数、类、组件或模块
- 不要给简单逻辑强行抽 `interface`、`helper`、`factory`、`manager`、`service`
- 不要把文件结构整理成过于标准的“状态区、生命周期区、事件区、方法区、工具区”
- 不要把所有连续空行、链式调用节奏都整理到完全统一

允许局部存在项目里常见的写法：
- 少量连续空行
- 简单逻辑就地书写
- 同文件里保留少量历史写法
- 与周围代码一致的局部不完美

如果用户或项目会自动格式化代码，不要依靠缩进混乱、空格混乱制造手写感。

## 4. 业务代码优先直接表达

开发时优先采用“当前文件直接完成当前职责”的方式。

如果一个逻辑只服务当前文件，优先写在当前文件里。

只有满足以下情况才拆出去：
- 多处复用
- 当前文件已经明显过长
- 有独立状态、生命周期或副作用
- 涉及外部依赖，需要隔离
- 拆出后能明显降低阅读成本

不要因为某个区域叫 `Header`、`Card`、`Service`、`Manager`、`Helper`、`Utils` 就自动拆文件或建类。

## 5. 命名保留项目习惯

命名优先贴近现有项目习惯，不追求教科书式完美。

允许：
- 历史遗留命名
- 项目私有缩写
- 已经广泛使用的业务简称
- 当前团队约定俗成的字段名、函数名、文件名

避免：
- `EnhancedXXX`
- `OptimizedXXX`
- `UnifiedXXX`
- `ComprehensiveXXX`
- `AdvancedXXX`
- `UniversalXXX`

命名应短、直、像项目里原本会出现的名字。

## 6. 不追求一次性完美

新增功能时优先完成业务闭环，不提前做大规模架构整理。

禁止顺手做：
- 重命名大量旧文件
- 统一全项目格式
- 把旧代码全部改成同一种模板
- 把临时业务数据强行迁移成复杂数据层
- 为未来可能需求提前设计扩展点
- 引入当前需求不需要的新框架、新模式、新目录结构

如果只是改一个文件，就只改这个文件相关代码。

## 7. 写法贴近已有项目

代码写法优先参考现有项目，而不是自动套用通用模板。

允许：
- 简单常量直接写在局部
- 与周围代码一致的参数组织方式
- 与项目一致的错误处理方式
- 与项目一致的同步、异步、回调或事件写法

但必须保证：
- 不影响编译或运行
- 不影响主要业务流程
- 不引入明显重复 bug
- 不破坏已有架构边界

## 8. 保留项目痕迹

优先复用项目已有工具、组件、服务、常量、日志、错误处理和调用方式。

不要重新设计一套“更标准”的工具层。

如果项目已有做法能解决问题，就不要另起炉灶。

## 9. 修改旧代码时只做必要整理

修改旧文件时：
- 可以修正当前需求相关代码
- 可以补最小必要类型或校验
- 可以修明显 bug
- 不主动清理所有空行、注释、格式、命名
- 不顺手重构无关代码

保持文件原本的书写节奏。

## 10. 质量底线

“手写感”不等于故意写差。

禁止为了不像生成代码而：
- 故意制造 bug
- 故意破坏类型安全
- 故意留下不可达逻辑
- 故意使用废弃 API
- 故意降低可维护性
- 故意隐藏错误或吞掉异常

最终标准：代码能编译、能运行、能维护，同时不像被统一模板批量生成。
