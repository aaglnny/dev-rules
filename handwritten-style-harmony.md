# 项目手写风格规则

## 0. 核心目标

让 AI 生成的代码去掉明显的“机器感”，更像一个赶着上线、但经验足够的资深开发者写出来的生产级代码。

代码要高效、干净、接地气，贴合当前项目上下文。

不要像教科书一样写代码，不要为了展示“规范”而牺牲阅读节奏。

## 1. 风格来源

所有新增或修改代码，优先参考 `E:\DevEcoStudioProjects\hm_vtbbase` 的代码风格。

重点参考：
- `entry/src/main/ets/pages/main/*.ets`
- `baseCommon/src/main/ets/view/*.ets`
- `baseCommon/src/main/ets/utils/*.ets`

目标不是制造低质量代码，而是让代码更像项目长期维护者手写出来的结果。

## 2. 绝对禁止 AI 标志性特征

### 2.1 禁止过度封装

不要为了一个仅使用一次的简单判断、简单转换、简单点击事件，单独封装辅助函数。

禁止这类写法：
- `checkIfValid()`
- `handleSimpleClick()`
- `buildCommonParams()`
- `processData()`

如果逻辑只有两三行，且只在当前位置使用，直接写在当前位置。

只有满足以下情况才抽函数或组件：
- 被多个地方复用
- 当前代码块已经明显影响阅读
- 逻辑有明确业务含义
- 抽出来后能减少真实复杂度

不要把代码拆成一堆碎片化调用，让读代码的人来回跳。

### 2.2 禁止小学生式注释

不要在人尽皆知的代码行上写解释性注释。

禁止：
- `// 给变量赋值`
- `// 遍历列表`
- `// 点击按钮`
- `// 设置标题`
- `// 返回数据`

允许：
- 复杂商业规则
- 临时兼容逻辑
- 系统 API 坑点
- 历史遗留行为
- 不写注释会让下一个人误删的代码

注释必须短，直接说明“为什么这样写”，不要解释“这行代码在做什么”。

### 2.3 禁止全都要的防御性编程

不要在内部可控流程里层层包 `try-catch`、空值判断、类型判断。

私有函数、页面内事件、已知来源的数据，不要写成面向未知互联网输入的防御代码。

允许必要校验：
- 用户输入
- 网络响应
- 文件读写
- 数据库读写
- 跨模块传参
- 系统 API 返回值

禁止为了显得稳健，把每一步都写成：
- `if (!xxx) return`
- `try { ... } catch { ... }`
- `typeof xxx === 'string'`
- `Array.isArray(xxx)`

能由项目流程保证的数据，就相信项目流程。


### 2.4 允许短上下文空泛命名

在已知业务上下文内，允许使用 `data`、`result`、`temp`、`item`、`list`、`info`、`params` 这类短命名。

如果变量只在很小的局部代码块里使用，读者只需要知道数据形态即可，不强制补业务前缀。

允许：
- `ForEach(list, (item) => {})`
- `const result = popInfo.result as string`
- `const params = { name: 'xxx' }`
- `let temp = value`

只有在以下场景才优先使用业务命名：
- 跨函数传递
- 多个同类型数据同时存在
- 变量生命周期较长
- 仅靠 `data`、`result` 已经看不出来源
- 错误日志或数据库操作需要明确业务上下文

示例：
- 有一个列表时用 `list`，有两个列表时，用 `albumList`、`noteList`
- 有多个结果时，用 `pickerResult`、`saveResult`
- 需要日志定位时，用 `musicUrl`、`filePath`

命名原则：短上下文允许看数据形态，长链路才补业务语义。


### 2.5 单一业务文件内允许短方法名

在单一业务页面或单一职责文件内，允许使用短方法名表达当前文件的核心动作。

如果文件本身已经提供了明确业务上下文，方法名不需要重复文件名、页面名或业务名。

允许：
- `deletePendingNote()` 可以命名为 `delete()`
- `handleSleepTimeResult()` 可以命名为 `start()`
- `saveNote()` 可以命名为 `save()`

原因：
- 当前文件已经说明业务场景
- 方法只在当前文件内部使用
- 读者不需要通过方法名重复确认业务归属
- 短方法名更接近手写代码习惯

只有在以下场景才使用完整业务方法名：
- 方法会被其他文件调用
- 当前文件里存在多个相似动作
- 短方法名会造成歧义
- 方法承担跨业务逻辑
- 日志、回调、路由等需要明确业务语义

示例：
- 单文件内只有一个保存动作，用 `save()`
- 单文件内只有一个删除动作，用 `delete()`
- 单文件内只有一个开始动作，用 `start()`
- 同时存在删除笔记、删除图片、删除专辑时，用 `deleteNote()`、`deletePhoto()`、`deleteAlbum()`

命名原则：文件名已经表达业务时，方法名只表达动作；业务边界变宽时，再补充业务名。

## 3. 禁止过度工整

不要为了“看起来规范”而做以下事情：
- 不要把每个小 UI 块都拆成独立组件
- 不要给简单逻辑强行抽 `interface`、`helper`、`factory`
- 不要把页面结构整理成过于标准的“状态区、生命周期区、方法区、Builder 区”
- 不要把所有连续空行、链式调用节奏都整理到完全统一

允许局部存在项目里常见的写法：
- 少量连续空行
- 简单页面内直接写 UI，不强制抽组件

注意：用户保存代码前会手动格式化，缩进和普通空格会被统一。因此不要依靠缩进混乱制造手写感。

## 4. 页面代码优先直接表达

页面开发优先采用“当前页面直接完成当前页面”的方式。

如果一个 UI 区域只服务当前页面，优先写在当前页面里。

只有满足以下情况才抽组件：
- 同一 UI 在多个页面复用
- 单个页面文件已经明显过长
- 子组件有独立状态或独立事件
- 抽出后能明显降低阅读成本

不要因为某个区域叫“Header”“Card”“Section”就自动拆成组件。

## 5. 命名保留业务习惯

命名优先贴近现有项目习惯，不追求教科书式完美。

允许：
- `Home01`、`Home02`、`ABCPage` 这类历史命名
- `title1`、`backImg`、`menuCallback` 这类已有业务命名

避免：
- `EnhancedSleepExperienceComponent`
- `OptimizedAlbumPresentationView`
- `UnifiedInteractionHandler`
- `ComprehensiveDataProcessor`

命名应短、直、像项目里原本会出现的名字。

## 6. 不追求一次性完美

新增功能时优先完成业务闭环，不提前做大规模架构整理。

禁止顺手做：
- 重命名大量旧文件
- 统一全项目格式
- 把旧页面全部改成同一种模板
- 把临时业务数据强行迁移成复杂数据层
- 为未来可能需求提前设计扩展点

如果只是改一个页面，就只改这个页面相关代码。

## 7. UI 写法贴近已有项目

ArkUI 链式调用优先参考现有写法。

允许数字、资源混用：
- 简单间距可直接写 `10`、`16`
- 已有资源继续用 `$r('app.float.vp_16')`
- 不强制所有颜色、字号都抽常量

但必须保证：
- 不影响编译
- 不影响主要适配
- 不引入明显重复 bug

## 8. 保留项目痕迹

优先复用项目已有工具、组件和写法：
- `CommonConstants`
- `BaseTitleBarV2`
- `VtbLogger`
- `BuilderNameConstants`
- 现有 `pathStack` 跳转方式
- 现有资源命名

不要重新设计一套“更标准”的工具层。

## 9. 修改旧代码时只做必要整理

修改旧文件时：
- 可以修正当前需求相关代码
- 可以补最小必要类型
- 可以修明显 bug
- 不主动清理所有空行、注释、格式、命名

保持文件原本的书写节奏。

## 10. 质量底线

“手写感”不等于故意写差。

禁止为了不像生成代码而：
- 故意制造 bug
- 故意破坏类型安全
- 故意留下不可达逻辑
- 故意使用废弃 API
- 故意降低可维护性

最终标准：代码能编译、能运行、能维护，同时不像被统一模板批量生成。



